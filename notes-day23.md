# Day 23：git blame（代码考古）

日期：2026-08-16

## 核心概念

- blame 逐行标注文件的"主人"：每一行属于哪个提交、谁、什么时候写的
- 团队用途：定位某行代码的作者、找 bug 引入的提交、理解"为什么这么写"
- 完整考古链：blame 定位行 → 记哈希 → `git show <哈希>` 看当时上下文

## 关键命令

```bash
git blame <文件>                # 每行标注提交/作者/日期/行号
git blame -L 起,止 <文件>       # 只看指定行范围
git blame <文件> --since="日期" # 只看某时间后的行
git blame -w <文件>             # 忽略空白变化（只看实质改动）
git show <blame中的哈希>        # 看该行提交当时改了什么
```

## 今日练习记录

- `git blame notes-day19.md`：看出 f1510ac4（原始版）和 bd355e8f（补充密钥泄露部分）两批行的分层归属
- `git blame -L 36,38` 只看指定行
- `--since="2 days ago"` 过滤时间
- 完整考古链：blame → bd355e8 哈希 → `git show` 看提交内容 → `log -S` 追文本变化

## 踩坑/结论

- blame 与 log 是姊妹工具：blame 按"行"看，log 按"提交"看
- 输出里的哈希可直接用于 git show，形成考古链
- 配 `-w` 忽略空白改动，避免把"改格式"误当"改逻辑"
- blame 只对已提交的内容有效，未提交改动不显示