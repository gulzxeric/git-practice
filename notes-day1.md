# Day 1：分支的本质

日期：2026-08-12

## 核心认知

- 分支不是目录副本，只是一个**指向提交的可移动指针**
- 创建分支 = 新建一个指针，不复制任何文件，所以极快
- `HEAD` 告诉 Git 当前在哪个分支工作
- 切换分支 = 移动 HEAD + 工作区内容跟着变

## 关键命令

```bash
git branch                # 列出所有分支（* 标记当前分支）
git branch -v             # 列出分支及其指向的提交
git switch -c <name>      # 创建并切换到新分支
git switch <name>         # 切换到已有分支
git symbolic-ref HEAD     # 查看 HEAD 指向哪个引用
```

## 今日练习记录

- 创建了 `feature/learning-branch` 分支
- 观察：创建分支后两个分支指向同一个提交 `65c4cba`
- 结论：分支分裂只发生在各自提交之后
  - 光执行“新建分支”这个动作，不会在历史轨迹里产生任何岔路；只有当你在新分支上提交了新代码，两条分支的发展轨道才会真正分离（分叉）。
