# Day 10：revert（安全撤销已推送提交）

日期：2026-08-13

## 核心概念

reset 和 revert 都能"撤销"，但适用场景不同：

- **reset**：移动指针、改写历史。仅适合本地、未推送的提交
- **revert**：不删历史，**新造一个反方向的提交**把改动抵消掉。适合**已推送、多人共享**的提交

revert 关键点：
- 历史没被改写，原坏提交留在历史里（可审计）
- 新增一个"撤销提交"，其他人 pull 时无感同步，零冲突
- 提交是向前走，不是向后退

## 关键命令

```bash
git revert <hash>          # 撤销某提交，生成反向提交
git revert --no-edit <hash> # 撤销时不打开编辑器（用默认信息）
git revert -m 1 <merge哈希> # 撤销合并提交时指定保留哪条边（1=主线）
```

## 今日练习记录

- 创建 app-config.md 并提交（模拟"误提交密钥"的坏提交 2d127eb）
- `git revert --no-edit 2d127eb` → 生成 Revert 提交 0bfa0c3，文件被删除
- 验证：原坏提交 2d127eb 仍在历史中，新提交只是反向抵消

## 踩坑/结论

- **决策口诀**：改自己的 → reset；改团队的 → revert
- revert 之后文件可继续修改重新提交，revert 不锁文件
- 撤销 merge 提交必须 `-m 1`（否则报错），普通提交不需要
- revert 生成的新提交也有哈希，它本身也可以被 revert（撤销"撤销"）

## 对比总结

| | reset | revert |
|---|---|---|
| 本质 | 改写历史（指针回退） | 新增反向提交 |
| 适用 | 本地、未推送 | 已推送、多人共享 |
| 对他人 | 历史错乱风险 | 无感同步 |
| 坏提交 | 从历史消失 | 留在历史里 |
