# Day 7：第 1 周复盘（分支）

日期：2026-08-13

## 本周知识地图

| Day | 主题 | 一句话记住 |
|-----|------|-----------|
| 1 | 分支本质 | 分支 = 指向提交的可移动指针；HEAD = 当前所在分支 |
| 2 | merge | 三种形态：fast-forward / 3-way / 冲突 |
| 3 | 冲突解决 | 读标记 → 编辑 → add → commit |
| 4 | stash | 工作区的储物柜，栈结构，与分支无关 |
| 5 | 分支整理 | `-d` 安全删（检查合并），`-D` 强制删 |
| 6 | rebase | 重写历史成直线，慎用于共享分支 |

## 本周自测结果

- 创建分支 = 创建指向提交的指针/标签 ✓
- `-d` 只删已合并，`-D` 强制删 ✓
- stash pop 取最新（后进先出）✓
- `UU` = unmerged 双方都改 ✓
- **纠正**：冲突后的"继续"——merge 用 `git commit`，rebase 用 `git rebase --continue`（`--abort` 是放弃）

## 本周踩坑清单（复用价值最高）

1. 冲突标记残留（`>>>>>>>`）可能悄悄进提交——解决后要检查
2. stash 同文件多次 pop 会撞冲突（先提交或处理 UU）
3. rebase --continue 会弹 vim 编辑器——用 `GIT_EDITOR=true` 跳过
4. 未合并分支删前要想清楚，`-d` 的拒绝是在保护你

## 结论

- merge 是团队标准做法，够用；rebase 是整理技巧，可后补
- 冲突不可怕，是 Git 在求助，4 步套路已形成肌肉记忆
- 仓库历史已是一张丰富分支图，可随时用 `git log --graph` 温习

## 下周预告（第 2 周：撤销与后悔药）

reset 三模式 → reflog（王牌）→ revert → restore → cherry-pick。学完你就敢乱操作了。
