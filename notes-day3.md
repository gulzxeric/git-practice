# Day 3：解决冲突（专项）

日期：2026-08-12

## 核心概念

冲突的本质：**两个分支在同一个位置做了不同的改动，Git 不知道听谁的**。

冲突判定（Day 2 复习）：
- 一方改了，另一方没动 → 自动采用，不冲突
- 双方都改但不同行 → 自动合并，不冲突
- 双方都改**同一行/重叠区**且不一致 → **冲突**

冲突类型：
- `UU`（content）：双方改了同一文件的同一区域
- `AA`（add/add）：双方各自新建同名文件

## 冲突解决四步（肌肉记忆）

1. `git status` 看哪些文件冲突（`UU` / `AA` 红色标记）
2. 打开文件，看 `<<<<<<< HEAD` / `=======` / `>>>>>>> <branch>` 标记
3. 编辑文件——保留想要的，删掉三行标记
4. `git add <file>` 声明已解决 → `git commit` 完成合并

## 关键命令

```bash
git merge <branch>     # 触发合并/冲突
git merge --abort      # 放弃这次合并，回到 merge 前
git status --short     # UU=双方都改且未解决, AA=add/add
git log --oneline --graph --all   # 看合并提交和分支图
git log --merges       # 只看合并提交
git rev-list --parents -n 1 <hash>  # 看提交的父提交（合并提交有2个）
```

## 今日练习记录

- 演示：同文件同行冲突（content 冲突），解决并产生合并提交 954ddd4
- 自行练习 story.md 冲突 2-3 轮，全部成功产生双父合并提交
- 提前用 `git merge --abort` 体验过放弃合并

## 踩坑/结论

- **冲突标记可能残留**：Day 2 的 day2-todo.md 里残留了 `>>>>>>>`，已提交后才发现并修复。教训：解决冲突后要检查文件里没有 `<<<<<<<`/`=======`/`>>>>>>>` 残留
- `git status` 里 `U` = **Unmerged（未合并）**，不是"修改"。`UU` = 双方都改且未解决
- 解决冲突必须**先 `git add` 再 `git commit`**，顺序反了报错
- 冲突不是错误，是 Git 在向你求助："这里我拿不准，你来决定"
